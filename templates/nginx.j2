 server {
        listen       80;
        listen       [::]:80;
        server_name  {{ app_name }}.reyesmtz.com ;
        
        include /etc/nginx/default.d/*.conf;

        location / {
            proxy_pass http://127.0.0.1:{{ red_servicio_puerto_contenedor }} ;
        }
    }
