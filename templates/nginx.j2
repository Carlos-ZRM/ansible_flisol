 server {
        listen       80;
        listen       [::]:80;
        server_name  _;
        
        include /etc/nginx/default.d/*.conf;

        location / {
            proxy_pass http://{{ red_servicio_ip_contenedor }}:{{ container_port }} ;
        }
    }
